
# Ring the bell if any background window rang a bell
set -g bell-action any
# At times, enabling mouse is useful
set -g mouse on

# Change hot key to C-a - easier to reach than default C-b
unbind C-b
set -g prefix C-a
bind C-a send-prefix
bind a last-window
bind c new-window -c "#{pane_current_path}"

# Create splits and vertical splits | (without shift) for vertical and minus for horizontal
bind-key \\ split-window -h -p 50 -c "#{pane_current_path}"
bind-key - split-window -p 50 -c "#{pane_current_path}"

# Panes and windows start at 1 and not 0
set -g base-index 1
setw -g pane-base-index 1

# Pane resize in all four directions using vi bindings.
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

bind-key ^D detach-client

# easier copy ops
setw -g mode-keys vi


set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'catppuccin/tmux'

set -g @catppuccin_flavor "frappe"
set -g @catppuccin_status_left_separator "█"

set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""

set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_host}"

run '~/.config/tmux/plugins/tpm/tpm'
